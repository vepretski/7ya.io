# Netlify Functions API changelog

## 2026-07-08

Production-ready scaffold added:

- static publish path kept at `packages/app/public`;
- Netlify functions path set to `netlify/functions`;
- `/api/health` route added;
- `/api/intake` route added;
- `/api/contact` alias added;
- validation script added;
- GitHub Actions validation workflow added;
- docs added for environment variables and deployment behavior.
