const esc=s=>String(s??'').replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c]))
const status=s=>`<span class="status">${esc(s)}</span>`
const card=item=>`<article class="card" data-provider="${esc(item.provider)}" data-type="${esc(item.type)}" data-language="${esc(item.language)}"><div><p class="provider">${esc(item.provider.toUpperCase())} / ${esc(item.type.toUpperCase())}</p><h3>${esc(item.title)}</h3>${item.subtitle?`<p>${esc(item.subtitle)}</p>`:''}<p>${esc(item.description||'')}</p></div><div><div class="meta-row"><span>${esc(item.publishDate||item.capturedDate||'NOT_AVAILABLE')}</span>${status(item.verificationStatus)}<span>${esc(item.relatedStoryCluster||'general')}</span></div><div class="actions"><a href="${esc(item.canonicalUrl)}" target="_blank" rel="noopener noreferrer">OPEN SOURCE</a><a href="/embed/${esc(item.provider)}/${esc(item.id)}">OPEN EMBED</a><button type="button" data-copy="${esc(item.canonicalUrl)}">COPY LINK</button></div></div></article>`
async function media(){const root=document.querySelector('[data-media-grid]');if(!root)return;const data=await fetch('/api/media').then(r=>r.json());const render=()=>{const q=(document.querySelector('#q')?.value||'').toLowerCase();const provider=document.querySelector('#provider')?.value||'';const type=document.querySelector('#type')?.value||'';const lang=document.querySelector('#lang')?.value||'';const verified=document.querySelector('#verified')?.checked;const found=data.items.filter(x=>(!provider||x.provider===provider)&&(!type||x.type===type)&&(!lang||x.language===lang)&&(!verified||x.verificationStatus==='VERIFIED_SOURCE')&&(!q||JSON.stringify(x).toLowerCase().includes(q)));root.innerHTML=found.length?found.map(card).join(''):'<p class="empty">NO MATCHING PUBLIC RECORDS</p>'};document.querySelectorAll('.filters input,.filters select').forEach(x=>x.addEventListener('input',render));render()}
async function evidence(){const root=document.querySelector('[data-evidence-grid]');if(!root)return;const data=await fetch('/api/evidence').then(r=>r.json());root.innerHTML=data.items.map(x=>`<article class="claim"><p class="provider">${esc(x.claimId)} / ${esc(x.status)}</p><h3>${esc(x.publicWording)}</h3><p>${esc(x.context)}</p><div class="meta-row"><span>${esc(x.sourceTier)}</span><span>${esc(x.date)}</span><span>${esc(x.lastReviewed)}</span></div><div class="actions"><a href="${esc(x.canonicalSource)}" target="_blank" rel="noopener noreferrer">OPEN SOURCE</a></div></article>`).join('')}
async function press(){const root=document.querySelector('[data-press-grid]');if(!root)return;const data=await fetch('/api/press').then(r=>r.json());root.innerHTML=data.items.map(card).join('')}
async function embed(){const root=document.querySelector('[data-embed-root]');if(!root)return;const match=location.pathname.match(/^\/embed\/([^/]+)\/([^/]+)$/);const data=await fetch('/api/media').then(r=>r.json());const item=match&&data.items.find(x=>x.provider===match[1]&&x.id===match[2]);if(!item){root.innerHTML='<p class="empty">PUBLIC MEDIA RECORD NOT FOUND</p>';return}const body=item.embedUrl?`<div class="embed-frame"><iframe loading="lazy" src="${esc(item.embedUrl)}" title="${esc(item.title)}" allowfullscreen></iframe></div>`:`<div class="note"><p>Stable inline embedding is not available for this record. The canonical public source remains accessible externally.</p></div>`;root.innerHTML=`<p class="provider">${esc(item.provider.toUpperCase())} / ${esc(item.verificationStatus)}</p><h1>${esc(item.title)}</h1><p>${esc(item.description||'')}</p>${body}<div class="actions"><a href="${esc(item.canonicalUrl)}" target="_blank" rel="noopener noreferrer">OPEN CANONICAL SOURCE</a></div>`}
document.addEventListener('click',e=>{const b=e.target.closest('[data-copy]');if(b)navigator.clipboard?.writeText(b.dataset.copy)});media();evidence();press();embed();
